# Validity checks on generated CIFs. INPUT is per-run, so pass --input as an override.
SCRIPT="scripts/eval/validate_steered_cifs.py"
VENV="crystallm"
QUEUE="batch"
TIME="4:00:00"
MEM="32G"
CPUS="16"
ARGS="--workers 16 --results-dir steering_results/density_atomic"
